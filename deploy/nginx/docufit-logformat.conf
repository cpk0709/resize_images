# DocuFit 접속 로그 형식. log_format 은 http 컨텍스트에서만 선언할 수 있어 conf.d 에 둔다 (setup 스크립트가 설치).
# 리퍼러·User-Agent 를 남기지 않는다. 사이트 파일(docufit*.conf)의 access_log 가 이 이름을 참조한다.
log_format docufit_min '$remote_addr - [$time_local] "$request_method $uri" $status $body_bytes_sent';
