# GitHub Pages 임시 배포. main 에 push 될 때마다 정적 export 를 빌드해 배포한다.
# 저장소 Settings → Pages → Source 가 "GitHub Actions" 여야 한다 (configure-pages 가 enablement 를 시도한다).
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci

      - name: Type check & lint
        run: npm run typecheck && npm run lint

      - name: Build static export (basePath /resize_images, api 라우트 제외)
        run: npm run build:pages

      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
